// Copy to env.js and fill in. env.js is gitignored.
// Only ever put PUBLIC values here. The Supabase publishable key is safe to
// expose ONLY because Row Level Security is doing the real work. Never put a
// service role key in this file or anywhere in this repo.
export const ENV = {
  SUPABASE_URL: "",
  SUPABASE_PUBLISHABLE_KEY: "",
};
